
        
        
        <div class="formulaire">
            <button class="button">
                <i class="material-icons">
                close
                </i></button>
            {{form_start(form,{attr:{class:'js-form'}})}}
            {{ form_widget(form)}}
            <input type="submit" class="btn " value="Editer"/>
            {{ form_end(form)}}
        </div>
 

